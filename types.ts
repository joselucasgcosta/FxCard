
export interface TextConfig {
  name: string;
  fontSize: number;
  fontColor: string;
  positionX: number;
  positionY: number;
  fontFamily: string;
  uppercase: boolean;
}

export interface Preset {
  id: string;
  name: string;
  url: string;
}
